let config = {
    appName: "jjzz", // 项目名称
    prefix: "RX_DATA_", //本地存储前缀
    secret: false, //本地存储是否加密
    secretKey: "12345678", //秘钥
    main: "main", //主程序项目（根目录下）
    outDirName: "dist", //打包输出文件夹名称
}

export default config;
